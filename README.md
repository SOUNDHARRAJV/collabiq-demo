# COLLABIQ

## Project Structure

- `client/` contains the React frontend.
- `server/` contains the Express and Socket.IO backend.
- `index.html` remains the Vite entry shell and loads `client/src/main.tsx`.


